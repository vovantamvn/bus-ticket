# Config for testing
spring:
  # Config MYSQL
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/db_bus_tickets_test
  # Config hibernate
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
